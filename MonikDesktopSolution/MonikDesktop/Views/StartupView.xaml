<UserControl x:Class="MonikDesktop.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MonikDesktop.Views"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:viewModels="clr-namespace:MonikDesktop.ViewModels"
             xmlns:interfaces="clr-namespace:MonikDesktop.Common.Interfaces"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="3">
            <Label>Window header</Label>
            <TextBox Text="{Binding Path=App.Title}" />
            <Label>Server Url</Label>
            <TextBox Text="{Binding Path=App.ServerUrl}" />
            <Label>Actions</Label>
            <Button Command="{Binding NewLogCommand}">Create Logs</Button>
            <Separator />
            <Button Command="{Binding ShowSpinnerCommand}">Create KeepAlive</Button>
        </StackPanel>
        <Grid Background="Gray" Opacity="0.75" Visibility="{Binding Path=ShowSpinner}">
            <fa:ImageAwesome Icon ="Cog" Spin="True" Height="48" Width="48" SpinDuration ="4" />
        </Grid>
    </Grid>
</UserControl>